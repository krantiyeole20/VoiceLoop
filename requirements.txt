# =============================================================================
# VoiceLoop — Python dependencies (pinned)
# Python 3.11 required.
# =============================================================================

# Core LiveKit
livekit>=1.0.19,<2
livekit-api>=1.0.7,<2
livekit-agents>=0.11.2

# LiveKit plugins
livekit-plugins-openai>=1.3.6
livekit-plugins-deepgram>=1.3.6
livekit-plugins-silero>=1.3.6
livekit-plugins-turn-detector>=0.4.0

# Flask
Flask==3.0.0
Flask-Cors==4.0.0
gunicorn==21.2.0

# Env / config
python-dotenv==1.0.0

# Async HTTP
aiohttp>=3.9.0

# Document processing
PyPDF2>=3.0.0
python-docx>=1.1.0

# Supabase + crypto
supabase==2.27.0
cryptography>=44.0.2
pyjwt==2.10.1

# OpenAI client (used by eval pipeline + feedback endpoints + question gen)
openai>=1.40.0

# MCP — used only by eval_mcp_server.py (Phase 4). Import lazily inside that module.
mcp>=1.0.0
